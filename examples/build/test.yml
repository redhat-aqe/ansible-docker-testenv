- hosts: localhost
  roles:
    - role: ansible-testing/setup
      vars:
        ansible_testing_hosts:
          - name: foo
            build: .
            image: localhost/ansible-testing-example-build
            command: sleep inf

- import_playbook: '{{ playbook_dir}}/playbook.yml'

- hosts: localhost
  roles:
    - role: ansible-testing/teardown
